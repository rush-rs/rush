/* let mut truth = false;

fn main() {
    set();
    exit(truth as int);
}

fn set() {
    truth = true;
} */

fn main() {
    let mut a = 2;
    a **= add_1(3);
    exit(a)
}

fn add_1(mut n: int) -> int {
    n += 1;
    n
}
