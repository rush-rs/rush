fn foo() {
    let b = 1.5;
    let a = 40.5 + 1.5 + b;
    //let c = a + b;
    //let c = (a +b) as int;
    exit(a as int)
}

fn a() {
    let a = if true {
        if true {
            let a = 42 + 40;
            a
        } else {
            43
        }
    } else {
        69
    };
    exit(a)
}

fn b() {
    let res = if false {
        let cod = 42;
        cod
    } else {
        let cod = 69;
        cod
    };
    exit(res)
}

fn c(n: int, m: int) -> int {
    let a = if m > n {
        -1 * (3 - 4)
    } else {
        2 - 1
    };
    -1 * (3 - 4)
}

fn main() {
    let res = a1(3) + a1(2);
    exit(res)
}

fn a2(n: int) -> int {
    3 + a1(n)
}

fn a1(n: int) -> int {
    3 + n
}

fn fib(n: int) -> int {
    if n < 2 {
        n
    } else {
        fib(n - 2) + fib(n - 1)
    }
}
